[pytest]
testpaths = tests
asyncio_mode = auto
# Each test gets a fresh event loop; the ingestion workers are per-loop tasks.
asyncio_default_fixture_loop_scope = function
filterwarnings =
    error::DeprecationWarning:app.*
